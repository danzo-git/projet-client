{% extends 'base.html.twig' %}

{% block title %}Gestion des Catégories{% endblock %}

{% block content %}
<div class="section">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    {% include 'layouts/_flash_message.html.twig' %}
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <h1>Gestion des Catégories</h1>
                            <a href="{{ path('app_category_create') }}" class="btn btn-primary">
                                <i class="fa fa-plus"></i> Nouvelle Catégorie
                            </a>
                        </div>

                        <div class="table-responsive">
                            <table id="categories-table" class="table table-striped table-bordered">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Nom</th>
                                        <th>Description</th>
                                        <th>Date de création</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {% for category in categories %}
                                        <tr>
                                            <td>{{ category.id }}</td>
                                            <td>{{ category.name }}</td>
                                            <td>{{ category.slug }}</td>
                                            <td>
                                                <div class="btn-group" role="group">
                                                    <a href="{{ path('app_category', {'id': category.id}) }}" class="btn btn-info btn-sm" title="Voir">
                                                        <i class="fa fa-eye"></i>
                                                    </a>
                                                    <a href="{{ path('app_category_update', {'id': category.id}) }}" class="btn btn-warning btn-sm" title="Modifier">
                                                        <i class="fa fa-edit"></i>
                                                    </a>
                                                    <form method="post" action="{{ path('app_category_delete', {'id': category.id}) }}" style="display: inline-block;" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette catégorie ?');">
                                                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
                                                        <button type="submit" class="btn btn-danger btn-sm" title="Supprimer">
                                                            <i class="fa fa-trash"></i>
                                                        </button>
                                                    </form>
                                                </div>
                                            </td>
                                        </tr>
                                    {% else %}
                                        <tr>
                                            <td colspan="5" class="text-center">Aucune catégorie trouvée</td>
                                        </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="https://cdn.datatables.net/1.11.5/css/dataTables.bootstrap5.min.css">
    <style>
        .card {
            margin-top: 20px;
            margin-bottom: 20px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        }
        .btn-group .btn {
            margin: 0 2px;
        }
        .table thead th {
            background-color: #1E1F29;
            color: white;
            border-color: #2B2D42;
        }
        .table-striped tbody tr:nth-of-type(odd) {
            background-color: rgba(0, 0, 0, 0.02);
        }
        .btn-primary {
            background-color: #D10024;
            border-color: #D10024;
        }
        .btn-primary:hover {
            background-color: #B10024;
            border-color: #B10024;
        }
    </style>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.11.5/js/dataTables.bootstrap5.min.js"></script>
    <script>
        $(document).ready(function() {
            $('#categories-table').DataTable({
                language: {
                    url: '//cdn.datatables.net/plug-ins/1.11.5/i18n/fr-FR.json'
                },
                pageLength: 10,
                order: [[0, 'desc']],
                responsive: true,
                columnDefs: [
                    {
                        targets: -1,
                        orderable: false
                    }
                ]
            });
        });
    </script>
{% endblock %}